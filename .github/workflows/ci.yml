name: CI

on:
  push:
    branches: [trunk]
  pull_request:
    branches: [trunk]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  ci:
    name: ci (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]
    steps:
      - uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.26'
          check-latest: true

      - name: Install dev tools
        # We install gofumpt / goimports here so `make fmt-check` works.
        # golangci-lint comes from its dedicated action below.
        run: |
          go install mvdan.cc/gofumpt@latest
          go install golang.org/x/tools/cmd/goimports@latest

      - name: Lint (golangci-lint)
        # v8 of the action defaults to golangci-lint v2.x, which is what
        # `.golangci.yml` (v2 schema) and our Go 1.26 build require. Older
        # action releases with `version: latest` resolve to the v1.x line
        # which is built with Go 1.24 and refuses our toolchain.
        uses: golangci/golangci-lint-action@v8
        with:
          version: v2.12.2
          args: --timeout=5m

      - name: Run make ci
        # `lint` target inside make ci is a no-op duplicate of the
        # golangci-lint-action above on CI; locally it's the canonical entry.
        # We still run it to exercise the rest of the pipeline (fmt-check,
        # vet, spdx-check, test, build).
        run: |
          make fmt-check
          make vet
          make spdx-check
          make test
          make build

      - name: Verify binary
        run: ./bin/shithub --version

  # Windows runs go test + build directly because the Windows runners
  # don't ship GNU make and our Makefile is the canonical entry on
  # POSIX. Lint / fmt / spdx run on the Linux+macOS leg above — those
  # checks are platform-invariant. This leg's job is catching the
  # filesystem / process semantics that bite on Windows (path
  # separators, file modes, exec.LookPath shims). Audit #154 added it.
  ci-windows:
    name: ci (windows-latest)
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.26'
          check-latest: true

      - name: go vet
        run: go vet ./...

      - name: go test
        run: go test ./...

      - name: go build
        run: go build -o bin/shithub.exe ./cmd/shithub

      - name: Verify binary
        run: .\bin\shithub.exe --version
